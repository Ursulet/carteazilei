export function SafePageContent({ content }: { content: string }) { const blocks = content.split(/\n{2,}/).map((block) => block.trim()).filter(Boolean); return <div className="prose-content">{blocks.map((block, index) => { if (block.startsWith("## ")) return <h2 key={index} className="mt-10 font-display text-3xl font-semibold">{block.slice(3)}</h2>; const lines = block.split("\n"); if (lines.every((line) => line.startsWith("- "))) return <ul key={index} className="mt-5 list-disc space-y-2 ps-6">{lines.map((line, lineIndex) => <li key={lineIndex}>{line.slice(2)}</li>)}</ul>; return <p key={index} className="mt-5 whitespace-pre-line leading-8 text-muted">{block}</p>; })}</div>; }
