import { notFound } from "next/navigation"; import { AdminPageHeader } from "@/components/admin/editorial-ui"; import { ConfirmDeleteForm } from "@/components/admin/confirm-delete-form"; import { NavigationItemForm } from "@/components/admin/navigation-item-form"; import { getAdminNavigationItem } from "@/domain/settings/navigation-service"; import { requirePermission } from "@/lib/auth/principal"; import { deleteNavigationAction, updateNavigationAction } from "../actions";
export default async function EditNavigationPage({ params }: { params: Promise<{ id: string }> }) { await requirePermission("navigation.manage"); const { id } = await params; const item = await getAdminNavigationItem(id); if (!item) notFound(); return <><AdminPageHeader eyebrow="Navigație" title={item.label} description="Actualizează poziția și destinația linkului." /><NavigationItemForm action={updateNavigationAction.bind(null, id)} values={item} /><div className="mt-8"><ConfirmDeleteForm action={deleteNavigationAction.bind(null, id)} /></div></>; }
