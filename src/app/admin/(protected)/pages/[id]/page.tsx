import { notFound } from "next/navigation"; import { AdminPageHeader } from "@/components/admin/editorial-ui"; import { ConfirmDeleteForm } from "@/components/admin/confirm-delete-form"; import { StaticPageForm } from "@/components/admin/static-page-form"; import { getAdminStaticPage } from "@/domain/editorial/static-page-service"; import { requirePermission } from "@/lib/auth/principal"; import { deletePageAction, updatePageAction } from "../actions";
export default async function EditPage({ params }: { params: Promise<{ id: string }> }) { await requirePermission("pages.manage"); const { id } = await params; const record = await getAdminStaticPage(id); if (!record) notFound(); return <><AdminPageHeader eyebrow="Pagini" title={record.page.title} description={`Pagina publică: /pagina/${record.page.slug}`} /><StaticPageForm action={updatePageAction.bind(null, id)} values={record.page} media={record.media} /><div className="mt-8"><ConfirmDeleteForm action={deletePageAction.bind(null, id)} /></div></>; }
