"use client";

export function UserSecurityActions({ userId, status, revokeAction, resetAction, inviteAction }: { userId: string; status: string; revokeAction: (id: string, formData: FormData) => Promise<void>; resetAction: (id: string, formData: FormData) => Promise<void>; inviteAction: (id: string, formData: FormData) => Promise<void> }) {
  return <section className="mt-8 rounded-2xl border border-border bg-surface p-5 sm:p-6"><h2 className="font-display text-2xl font-semibold">Securitate și sesiuni</h2><p className="mt-2 text-sm leading-6 text-muted">Operațiile de mai jos sunt auditate și invalidează imediat sesiunile existente.</p><div className="mt-5 flex flex-wrap gap-3"><form action={revokeAction.bind(null, userId)}><button className="min-h-11 rounded-full border border-border px-5 text-sm font-semibold hover:border-brand">Revocă sesiunile</button></form><form action={resetAction.bind(null, userId)}><button className="min-h-11 rounded-full border border-border px-5 text-sm font-semibold hover:border-brand">Forțează resetarea parolei</button></form>{status === "invited" ? <form action={inviteAction.bind(null, userId)}><button className="min-h-11 rounded-full border border-border px-5 text-sm font-semibold hover:border-brand">Generează altă invitație</button></form> : null}</div></section>;
}
