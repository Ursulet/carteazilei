import { AdminPageHeader } from "@/components/admin/editorial-ui"; import { StaticPageForm } from "@/components/admin/static-page-form"; import { getAdminMedia } from "@/db/queries/admin-media"; import { requirePermission } from "@/lib/auth/principal"; import { createPageAction } from "../actions";
export default async function NewPage() { await requirePermission("pages.manage"); const media = await getAdminMedia(); return <><AdminPageHeader eyebrow="Pagini" title="Pagină nouă" description="Creează conținut sigur, publicabil și cu metadata proprie." /><StaticPageForm action={createPageAction} media={media} /></>; }
